# OptiGraphMigrator report

0 error(s), 1 warning(s), 1 info

- Exact (auto-fixable): 1
- Caveat (needs review): 0
- Blocked (no clean translation): 1

## Top blocking patterns

| Rule | Title | Occurrences |
|---|---|---|
| OGM202 | Custom scoring has no Graph equivalent | 1 |

### Suggested approaches

These patterns have no Optimizely Graph equivalent, so the behaviour has to be re-implemented. Suggested starting points:

- **OGM202**: Optimizely Graph has no per-query boost function. Retrieve the candidate set from Graph, then apply the boost weights in application code when ordering the results.

## Findings

| Severity | Rule | Location | Message | Graph equivalent |
|---|---|---|---|---|
| warning | OGM001 | FindUsageSamples.cs(10,5) | Find 'Filter(...)' can be translated to a Graph 'where' clause | `where` |

  ```graphql
  { where: { title: { eq: "foo" } } }
  ```

| info | OGM202 | FindUsageSamples.cs(20,8) | Find custom scoring 'Boost(...)' has no clean Graph SDK translation |  |

> **Suggested approach:** Optimizely Graph has no per-query boost function. Retrieve the candidate set from Graph, then apply the boost weights in application code when ordering the results.
